import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://prevter.github.io/bindings-meta',
    base: '/bindings-meta',
    trailingSlash: 'always',
});
